{
  "name": "vocabcache-apk",
  "version": "1.0.0",
  "description": "VocabCache Android APK build",
  "scripts": {
    "copy-assets": "node copy-web-assets.js",
    "set-icon": "node set-icon.js",
    "sync": "node sync-apk.js",
    "build": "npm run copy-assets && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "sharp": "^0.33.0"
  }
}
